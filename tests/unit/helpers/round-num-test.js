import { roundNum } from 'swole-baby/helpers/round-num';
import { module, test } from 'qunit';

module('Unit | Helper | round num');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = roundNum([42]);
  assert.ok(result);
});
